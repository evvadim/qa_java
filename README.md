# Sprint 6 Project
Финальный проект 6 спринта

## Технологии
- IntelliJ IDEA 2025.1.2
- JDK corretto-11.0.27
- Build System Maven
- JUnit 4.13.2
- Mockito 5.14.2
- Jacoco 0.8.12

## Обязательное задание
1. Класс `Lion` отвязан от `Feline`-зависимости
2. Написаны моки для классов `Feline` и `Lion` (пакет `mocks`)
3. Написаны тесты для классов  `Feline`, `Cat` и `Lion` (пакет `common`)
4. Параметризация использовалась для класса `Animal` (пакет `param.animal`)
5. По оценке плагина Jacoco покрытие кода составляет 100% для каждого класса

## Дополнительное задание
1. Написан класс-наследник для льва Алекса
2. Написан конструктор
3. Добавлены методы `getFriends()` и `getPlaceOfLiving()`
4. Переопределен метод `getFitten()`, теперь он возвращает `0`
5. Класс покрыт тестами

## Запуск тестов
```
mvn clean test
```

## Генерация отчета Jacoco
```
mvn verify
```